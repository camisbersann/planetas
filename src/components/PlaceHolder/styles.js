import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    placeHolder: {
        fontWeight: "600",
        color: "#fff",
        textAlign: "left",
        marginTop: 10,
        marginBottom: 2,
    }
})

export default styles;