import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    text:{
      color: "#fff",
      textAlign: "center",
     fontSize: 16,
     marginTop:10,
     marginBottom:10,
     marginLeft:5,
     marginRight:5,
    },

    card:{
       marginTop: 20,
       marginBottom: 20,
       borderColor: "#EE82EE",
       borderWidth: 1,
       borderRadius:8,
    }
})
export default styles;